//动画库
import {TweenMax, Power2, TimelineLite} from "gsap/TweenMax";
class Menu{
    constructor({ el, duration, delay, ease = Quart.easeInOut } = options) {
        this.$container = $(el)
        this.$duration = duration
        this.$delay = delay
        this.$menuChild = this.$container.children()
    }
    init() {
        
        
    }

    entry() {


        // TweenMax.
    }
}