//动画库
import {TweenMax, Power2, TimelineLite} from "gsap/TweenMax";

class MenuButton {
    constructor() {

        that.showMenu = false //是否显示主菜单
        that.menuButton = $('.menu-button')
        let menuBg = that.menuButton = $('.menu-bg')
        let MenuTime = 0.3 // 菜单按钮动画时间
    }
}